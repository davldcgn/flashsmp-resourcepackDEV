# flashsmp-resourcepackDEV

Resourcepack fuer den FLASH-SMP-Scoreboard-Hintergrund (Test-/Dev-Stand,
gehoert zu `FlashScoreboardTest`).

- `resourcepack/` — Quelle (pack.mcmeta, Font-JSON, Textur)
- `flashsmp-scoreboard.zip` — fertig gepackt, direkt per URL nutzbar:
  ```
  https://raw.githubusercontent.com/davldcgn/flashsmp-resourcepackDEV/main/flashsmp-scoreboard.zip
  ```

SHA1 der zip: `dbf6370e8d50e3e1915c7e9274a0b6e0725e0dc0`

Aendert sich die zip (Bild/Font-JSON angepasst), muss sie hier neu
committet und der SHA1 in `FlashScoreboardTest/config.yml` aktualisiert
werden.
